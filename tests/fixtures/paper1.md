# Minimal Model Architecture

## Abstract
We propose a new model called MinimalNet. It uses a very simple architecture.

## 3 Method
The MinimalNet follows a standard feed-forward pattern.

$$\text{Output} = WX + b$$

The parameters $W$ and $b$ are learned during training.
